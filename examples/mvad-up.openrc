#!/sbin/openrc-run

description="mvad tunnel"
relay="${relay:-se-got-wg-001}"
command="/usr/bin/mvad"
command_args="up ${relay}"
command_background="yes"
pidfile="/run/mvad-up.pid"
retry="TERM/30/KILL/5"

depend() {
	need net
}
